global
	maxconn 100
	stats socket /tmp/sock1 mode 666 level admin
	stats timeout 2m

defaults
	timeout client 5s
	timeout server 5s
	timeout connect 5s
	mode http

listen stats
	bind :8888
	stats uri /

backend tcp9000
	option tcp-check
	server srv 127.0.0.1:9000 check inter 1s
