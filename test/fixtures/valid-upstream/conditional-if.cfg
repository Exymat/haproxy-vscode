global
    maxconn 1000

defaults
    mode http
    timeout client 30s
    timeout server 30s
    timeout connect 5s

frontend web
    bind :80
    .if !defined(HAPROXY_MWORKER)
    default_backend app
    .else
    default_backend app
    .endif

backend app
    server s1 127.0.0.1:8080 check
